# RIDOS-Core — removeuser.conf
# Place at: /etc/calamares/modules/removeuser.conf
# Removes the default live session user from the installed system.

---

# The username used in your live session (change if different)
username: user
